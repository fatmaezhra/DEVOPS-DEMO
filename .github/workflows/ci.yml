name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flask

    - name: Run tests
      run: |
        echo "No tests yet, just checking CI works"
        python -c "print('Tests passed')"

    - name: Deploy to Render
      run: |
        echo "Déploiement simulé ici"
        # Tu peux utiliser Render CLI ou autre pour vrai déploiement